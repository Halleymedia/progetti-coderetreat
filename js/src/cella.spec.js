'use strict';
describe("Regole per il cambiamento di stato della cella",
  function() {

    //Questo è un test di esempio
    it("verifica che true sia uguale a true", function () {
      //Se voglio richiamare i file di codice, uso require. Ad esempio:
      //const cella = require('./cella');
      
      expect(true).toEqual(true);
    });
  }
);